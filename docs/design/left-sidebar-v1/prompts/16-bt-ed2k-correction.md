Correct this Rayburst design reference sheet. It is meant to show TWO SEPARATE SCREENS, not one app with multiple permanent columns. Recompose as a plain white design sheet with two separately bordered app-content crops side by side and visible 40px white gutter. Label outside left crop "BitTorrent settings", outside right crop "eD2k search". Remove the invented second navigation sidebar from the left crop entirely; preserve only its compact title "BitTorrent" and all existing settings, trackers and apply control, using the newly freed width. The right crop remains the eD2k search with the same results and controls. Each crop is independent, not connected by a shared top bar. No extra navigation column. Preserve exact flat white / pale neutral / purple visual style and all substantive field values. Keep readable text at the same scale.

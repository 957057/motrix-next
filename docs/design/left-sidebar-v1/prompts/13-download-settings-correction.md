Edit this existing Rayburst settings mockup with ONLY these corrections. Preserve all other geometry, rows, controls, logo and style unchanged. 1) Active tab must be "下载与存储", purple text and underline; "高级" must be neutral unselected with no underline. 2) In the helper for 同时下载任务数 change the incorrect range "(1 - 10)" to "(1 - 100)". 3) In the helper for 每个任务连接数 change the incorrect range "(1 - 32)" to "(1 - 256)". 4) Default download directory value should be simply "Downloads" instead of an invented Administrator path. Do not add fields or rearrange layout. These are exact text/selection corrections for engineering fidelity.

# Scene 02 generation prompt

Generator: built-in ImageGen.

Use case: ui-mockup.
Create ONE high fidelity Rayburst desktop app screenshot, landscape 1600x1000, scenario 2: user creating a new download.
Image 1 is the EXACT shared shell and visual language to preserve. Image 2 is the actual Rayburst brand logo. Keep precisely the same sidebar width, wordmark with small purple logo, navigation rows, footer, top toolbar and the same comfortable typography scale as image 1. This is a second state of that same app, not a different design.
Visual language: quiet LIGHT opaque white content, very pale neutral sidebar, brand Electric Purple #7B3ED1, thin gray borders, restrained outline icons, flat list, no glass, no translucency in surfaces, no heavy gradients, no giant pill cards, no illustrations, no mockup surroundings. Render edge-to-edge straight-on app window; tiny minimize/maximize/close at upper-right. Clean Chinese sans-serif text.

Background is the existing all-tasks screen: logo + Rayburst upper-left, sidebar 全部任务 selected, 进行中 3, 已完成 2, 需要处理 1, bottom 浏览器连接 and 设置. Main header 全部任务 with search and 新建下载. Keep the recognisable lightweight download list behind a subtle uniform neutral 12% dimming overlay; do NOT blur it or turn it dark. Keep logo and settings recognisable.

Foreground: one beautifully proportioned opaque WHITE new-download dialog, approximately 680px wide at this image scale, vertically centered, soft small 12px corner radius, fine border and restrained small shadow. Comfortable balanced spacing and readable form, no floating oversized panels. Title "新建下载" at top left, close x at top right. Short tab row immediately below "链接" selected with a thin purple underline; "种子文件" unselected.
Form:
label "下载链接"
multiline input with exact URL "https://releases.ubuntu.com/24.04/ubuntu-24.04-desktop-amd64.iso" wrapped neatly to two lines if needed. Small clipboard icon button at top-right INSIDE the input. Secondary helper beneath "支持多条链接，每行一条".
label "保存到"
outlined path selector with folder icon, "Downloads / Linux", a clearly labeled small "更改" button on the right.
label "文件名"
single line input "ubuntu.iso" with no unnecessary adornment.
A thin divider and collapsed disclosure row "高级选项" with right chevron, secondary summary "连接、请求头与身份验证".
Bottom dialog footer has plain "取消" button followed by brand-purple solid "开始下载" button, right aligned. No invented file size or download progress inside dialog before submission. No success toast. No second dialog. Avoid anything resembling a webpage hero, no explanatory captions outside the app.

Ensure the main app behind stays at the same download state and same layout as reference. The dialog should feel calm and easy to scan, show only necessary fields, exactly one clear primary action. All visible labels crisp and legible.
